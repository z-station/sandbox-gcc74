version: "3.3"

services:

  sandbox-gcc74:
    build:
      context: ..
      dockerfile: ./docker/Dockerfile
    container_name: sandbox-gcc74
    image: sandbox-gcc74
    volumes:
      - ../src:/app/src:ro
    ports:
      - "8002:8002"
    restart: on-failure
